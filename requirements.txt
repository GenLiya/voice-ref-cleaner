# voice-ref-cleaner 核心依赖
# 系统依赖：ffmpeg 必须在 PATH 里
numpy>=1.24
soundfile>=0.12
librosa>=0.10
torch>=2.0
torchaudio>=2.0
scikit-learn>=1.3
demucs>=4.0          # 去 BGM（https://github.com/facebookresearch/demucs, MIT）
silero-vad>=5.0      # 语音区间检测（https://github.com/snakers4/silero-vad, MIT）
